// Middleware temporariamente desabilitado para resolver problemas de compatibilidade
// Será reativado após configuração completa do banco de dados

export function middleware() {
  // Middleware vazio por enquanto
}

export const config = {
  matcher: []
} 